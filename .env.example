# Environment variables template
# Copy this file to .env and fill in your actual values

# Flask Configuration
FLASK_SECRET_KEY=your-secret-key-here-change-this-in-production
FLASK_ENV=development

# Database Configuration
# For local development with SQLite (default if not set):
# DATABASE_URL=sqlite:///app.db

# For local development with PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/rsanju

# For Render deployment (set this in Render dashboard):
# DATABASE_URL=<your-render-postgresql-url>

# Firebase Configuration (JSON string from firebase_credentials.json)
# FIREBASE_CREDENTIALS={"type":"service_account","project_id":"..."}
